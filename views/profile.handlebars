

<!DOCTYPE html>
<html>

<head>
  <title>User Profile</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bulma/0.9.2/css/bulma.min.css"
    integrity="sha512-7SzrJ+8x04UV/svJoDYRtFTejhRzX8WJG+yzvYs1sLDBpCJ8WMMojg+GZfJFZiPzOT9CJrmGbz98/2dYg0U+Jg=="
    crossorigin="anonymous" referrerpolicy="no-referrer" />
</head>

<body>
  <section class="hero is-primary is-bold">
    <div class="hero-body">
      <div class="container is-pulled-left">
        <h1 class="title">User Profile</h1>
        <h2>Name: {{name}}<br>
          Email: {{email}}<br></h2>
      </div> 
      <button id="home-button" class="button is-pulled-right">Home</button>
    </div>
  </section>

      <div class="column has-text-centered">
        <a href="./upload/cpu" class="button is-primary is-medium has-text-right ">Upload CPU</a>
        <a href="./upload/graphics" class="button is-primary is-medium has-text-right ">Upload Graphics Card</a>
        <a href="./upload/memory" class="button is-primary is-medium has-text-right ">Upload Hard Drive</a>
      </div>
    </div>
  </section>

  <section class="section">
    <div class="container">
      <h2 class="title is-3 is-center"> Your Posts </h2>
      <h2 class="title is-4">CPU Posts</h2>
      <ul>
        {{#each items.cpuData}}
        <li>
          <strong>{{this.name}}</strong><br>
          <small>{{this.description}}</small><br>
          <span class="tag is-primary">{{this.brand}}</span><br>
          Our Price: ${{this.ourPrice}}<br>
          MSRP: ${{this.MSRP}}<br>
          Cores: {{this.cores}}<br>
          Threads: {{this.threads}}<br>
        </li>
        {{/each}}
      </ul>

      <h2 class="title is-4">Graphics Card Posts</h2>
      <ul>
        {{#each items.gpuData}}
        <li>
          <strong>{{this.name}}</strong><br>
          <small>{{this.description}}</small><br>
          Our Price: ${{this.ourPrice}}<br>
          MSRP: ${{this.MSRP}}<br>
          Clock Speed: {{this.clockSpeed}}<br>
        </li>
        {{/each}}
      </ul>

      <h2 class="title is-4">Memory Posts</h2>
      <ul>
        {{#each items.hardDriveData}}
        <li>
          <strong>{{this.name}}</strong><br>
          <small>{{this.description}}</small><br>
          Our Price: ${{this.ourPrice}}<br>
          MSRP: ${{this.MSRP}}<br>
          Storage Capacity: {{this.storageCapacity}}<br>
        </li>
        {{/each}}
      </ul>
    </div>
  </section>
</body>

</html>
<script src="/js/homepagebutton.js"></script>