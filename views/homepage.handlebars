{{> banner}}
<form class="search-form">
<input class="input is-primary" id="search-form" type="text" placeholder="Primary input">
<div class="buttons">
  <button class="button is-primary" id="search-button">Search</button>
</div>
</form>
{{#each items as |item|}}
<div class="card">
  <header class="card-header">
    <p class="card-header-title">
      Name: {{item.name}}    Price: {{item.ourprice}}    Date: {{item.date}}
    </p>
    <button class="card-header-icon" aria-label="more options">
      <span class="icon">
        <i class="fas fa-angle-down" aria-hidden="true"></i>
      </span>
    </button>
  </header>
  <div class="card-content">
    <div class="content">
      Description: {{item.description}}
    </div>
  </div>
  {{!-- <footer class="card-footer">
    <a href="#" class="card-footer-item">Upload</a>
    <a href="#" class="card-footer-item">Edit</a>
    <a href="#" class="card-footer-item">Delete</a>
  </footer> --}}
</div>
{{/each}}

<script src="js/homepage.js"></script>